{
  "name": "corefit-android",
  "version": "1.0.0",
  "private": true,
  "description": "Corefit - receita diária de whey com psyllium, assistente diário e cálculo calibrável",
  "scripts": {
    "sync": "npx cap sync android",
    "build:android": "npm install && npx cap add android && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@capacitor/local-notifications": "^7.0.0",
    "@capacitor/browser": "^7.0.0",
    "@capacitor/app-launcher": "^7.0.0",
    "@capacitor/filesystem": "^7.0.0",
    "@capacitor/file-transfer": "^1.0.0",
    "@capawesome-team/capacitor-file-opener": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
